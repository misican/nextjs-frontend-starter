# Skills

Each subdirectory is an agent skill loaded by GitHub Copilot and Cursor via symlinks/junctions from their respective discovery folders.

- `.github/skills/` → `.ai/skills/` (Copilot)
- `.cursor/skills/` → `.ai/skills/` (Cursor)

To add a skill: create `<name>/SKILL.md` here — no IDE config changes needed.
